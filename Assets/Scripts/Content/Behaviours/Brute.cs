using System.Collections.Generic;

namespace Gamepackage
{
    public class Brute : Behaviour
    {
        public override List<EntityAction> GetNextActions()
        {
            return new List<EntityAction>(0);
        }
    }
}
