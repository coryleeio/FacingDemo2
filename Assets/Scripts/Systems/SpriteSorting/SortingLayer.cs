namespace Gamepackage
{
    // *** STOP *** If you update these update the sorting layers in the game engine as well
    // You may need to adjust the overlay prefab sorting layer as well.
    public enum SortingLayer
    {
        Default,
        Ground,
        Overlays,
        EntitiesAndProps,
    }
}
